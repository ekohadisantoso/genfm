<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a987genfm.genfm">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity2" />
        <activity
            android:name=".MainActivity" android:launchMode="singleInstance"
            android:theme="@style/NoTitleBar" />
        <activity
            android:name=".ActivityTwo" android:launchMode="singleInstance"
            android:theme="@style/NoTitleBar" />
        <activity
            android:name=".ActivityThree" android:launchMode="singleInstance"
            android:theme="@style/NoTitleBar" />
        <activity
            android:name=".ActivityFour" android:launchMode="singleInstance"
            android:theme="@style/NoTitleBar" />
        <activity android:name=".FacebookActivity" />
        <activity android:name=".TwiterActivity" />
        <activity android:name=".WebsiteActivity" />

        <service
            android:name=".RadioService"
            android:exported="false" />

        <activity android:name=".Main3Activityvghg"></activity>
    </application>

</manifest>